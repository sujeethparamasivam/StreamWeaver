# Leave MONGO_URI unset to auto-start an in-memory MongoDB (mongodb-memory-server) — zero setup.
# Set it to point at a real MongoDB instance for persistent storage.
MONGO_URI=

# Change this in any real deployment.
JWT_SECRET=devsecret

PORT=5000
